export interface SoopshtenijaCardType {
  id: string,
  title: string,
  date: string,
  category: string,
  description: string
}

export interface NastaniCardType {
  img: string;
  title: string;
  paragraph: string;
  date: string;
}

